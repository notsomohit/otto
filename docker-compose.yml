version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    env_file:
      - ./backend/.env
    restart: unless-stopped
    networks:
      - otto-network

networks:
  otto-network:
    driver: bridge
